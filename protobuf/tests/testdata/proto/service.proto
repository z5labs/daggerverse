edition = "2023";

package test;

option go_package = "dagger/protobuf-tests/testdata/protopb";

service Echo {
    rpc Echo (Request) returns (Response);
}

message Request {
    string msg = 1;
}

message Response {
    string msg = 1;
}